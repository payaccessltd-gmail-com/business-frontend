export const config = {
  // matcher: ["/profile"],
  // matcher: [
  //   "/((?!register|api|login|registration|email-verification|transaction|home|reset-password|forget-password|otp|reset-success).*)",
  // ],
  matcher: [],
};

export { default } from "next-auth/middleware";
